/**
 * assets-catalog
 * Static asset catalog generator
 */

// Export runtime functions (for Vite environment)
export { createAssets, type InferAssets, type InferAssetMeta } from './runtime.js'

// Export types
export type { AssetType, AssetMeta, AssetTree } from './types.js'

